#include<iostream>
#include<algorithm>
#include <string.h>
using namespace std;

int main() {

    // string str = "college";
    // string str1("wallah");
    // cout << str << " " << str1 << endl;

    // string s;
    // getline(cin, s); 
    // cout << s;

    // Inbuilt functions
    // Reverse
    // string s = "hello";
    // reverse(s.begin(), s.end());
    // cout << s;

    // substr
    // string s = "survive";
    // cout << s.substr(1,4) << endl;
    // cout << s.substr(1) << endl;
    // cout << s.substr(6) << endl;

    // +
    // string s1 = "college ";
    // string s2 = "wallah ";
    // s1+=s2; // s2 gets append back of s1, no copy is created, no extra space is used
    // cout << s1 << endl;
    // cout << s1+s2 << endl;
    // s1 = s1+s2; // copy of s1 is created and s2 gets append at it's back
    // cout << s1 << endl;

    // strcat -> to concatenate char array
    // char arr1[10] = "college ";
    // char arr2[10] = "wallah";
    // strcat(arr1,arr2);
    // cout << arr1 << endl;

    // Pushback
    // string s = "abcd";
    // char ch = 'e';
    // s.push_back(ch);
    // cout << s << endl;
    
    // length
    // string s = "abcd";
    // cout << s.size() << endl;  // O(1)
    // cout << s.length() << endl;
    // char str[10] = "abcdefgh";  
    // cout << strlen(str) << endl; //O(n)

    // to_string -> to convert numeric value in string
    // int num = 432;
    // string s = to_string(num);
    // s += '1';
    // cout << s << endl;
    // cout << s[1] << endl;
    // s[1] = '9';
    // cout << s << endl;

    return 0;
}